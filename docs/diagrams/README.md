# Diagrams

- `deployment-flows.md` shows the branch, validation, and deployment lane flow.
- `keyvault-target-map.md` shows how `kv-dev`, `kv-qa`, and `keys` map to GitHub Environments.

GitHub renders Mermaid diagrams directly in Markdown.
